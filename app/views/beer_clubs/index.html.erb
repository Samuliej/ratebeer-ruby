<div class="centered-page-container">
  <% content_for :title, "Beer clubs" %>

  <h1>Beer clubs</h1>

  <%= link_to "New beer club", new_beer_club_path, class: "btn btn-primary" if current_user %>

  <div id="beer_clubs">
    <%= render "beer_club_table", beer_clubs: @beer_clubs %>
  </div>
</div>
